services:
  llm-gateway:
    build: .
    container_name: llm-gateway
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
      - ./secrets:/app/secrets:ro
    restart: unless-stopped
